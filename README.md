### Description

This is a code that output a dot file represent an $r$-regular graph.

### Usage

Compile Main.cpp with C++ (recommended C++ 17 or newer)

If you use Windows, you can directly run Main.exe.

The outputted dot file can be run using many softwares. My recommendation is Gephi with Force Atlas Layout.